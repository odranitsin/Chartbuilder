// Typography

// Fonts
$font-sans = 'PTS',Arial,Helvetica,sans-serif
$font-sans-light = 'PTS-Narrow',Arial,Helvetica,sans-serif
$font-sans-bold = 'PTS-Bold', Arial, sans-serif
$font-serif = Georgia,serif
$primary-font-family = $font-sans
$secondary-font-family = $font-serif
$monospaced-font-family = Monaco, Lucida Console, monspace


